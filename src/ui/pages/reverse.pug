include ../elements/meta
include ../elements/scripts
include ../elements/navigation

doctype html
html(lang='ru')
    head
        +meta
    body
        +navigation

        .container
            .row.justify-content-center
                .col-10.text-center
                    | TODO -

        +scripts